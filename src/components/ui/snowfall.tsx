"use client";

import ReactSnowfall from "react-snowfall";

const Snowfall = () => {
  return (
    <ReactSnowfall
      color="white"
      style={{
        position: "fixed",
      }}
    />
  );
};

export default Snowfall;
